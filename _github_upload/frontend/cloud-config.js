/* ==============================================================
   cloud-config.js — 云服务公开配置
   --------------------------------------------------------------
   部署到托管平台后, endpoint / publishableKey 由平台环境注入。
   本地自托管运行时保持 null, 前端会自动回退到服务端生成通道。
   ============================================================== */
window.WB_CLOUD_CONFIG = window.WB_CLOUD_CONFIG || null;
